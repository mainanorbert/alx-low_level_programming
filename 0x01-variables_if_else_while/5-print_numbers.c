#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Return: the program returns 0
 */
int main(void)
{
	int x = 0;

	while (x < 10)
	{
		printf("%d", x);
		x++;
	}
	printf("\n");
	return (0);
}
