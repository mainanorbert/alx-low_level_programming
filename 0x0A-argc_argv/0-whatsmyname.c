#include <stdio.h>

/**
 * main - prints name of program
 * @argc: argument count
 * @argv: array with no of arguments
 * Return: returns an integer 0 for success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0 * argc]);
	return (0);
}
